name: Delete
on:
  # schedule:
    # - cron： “0 0 * * 太阳”

jobs:
  del_runs:
    runs-on: ubuntu-latest
    步骤：
      - name: Delete
        uses: GitRML/delete-workflow-runs@main
        with:
          token: ${{ github.token }}
          repository: ${{ github.repository }}
          retain_days: 30
